*~
.iocsh_history
*.log
issue
work
# SDLC review-session artifacts are kept local-only (audit trail on disk).
docs/sdlc/*/review_sessions/
docs/*/review_sessions/
docs/review_sessions/
# Makefile site/release overrides (per-host, uncommitted).
configure/*.local
RELEASE.local
CONFIG_SITE.local
