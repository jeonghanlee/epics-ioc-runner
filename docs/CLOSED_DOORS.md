# Closed Doors

> Write down the doors you decided to leave closed. The open ones announce
> themselves; the closed ones only cost you twice.
>
> — *The Door You Left Closed*, <https://github.com/jeonghanlee/essay-site>

Findings that a coherence sweep examined and deliberately left as they were.
Each row is one line and a commit: the reasoning lives in that commit, not
here. The list does not hold the argument — it gives the next sweep something
to read in one pass, so a settled question is recognized before it is derived
again from the start.

This file is not tied to a release cycle. Closed release registers leave the
active tree after their final state is committed; this file does not.

**Promotion test** — default is Keep; promotion to an enforced guard runs four
ordered gates, and elimination is tried before guarding: `3e47ee6`.

| ID | Examined | Verdict | Commit |
| --- | --- | --- | --- |
| CI-25 | procServ unit template kept as two copies rather than one emitter (#81) | Keep, guarded by the shared-contract test | `040f32f`, guard `7a3aeb2` |
| CI-26 | System service identity resolved independently in both scripts (#87) | Keep, pinned by the static identity guard | `96fc886` |
| CI-27 | Unify the `resolve_sock_path` callers (#86) | Keep B — the drift would be cosmetic, gate B fails | `ee82e09` |
| CI-28 | Do the `cmp`-identical skip paths leave a hand-loosened mode un-reasserted? Examined during the M3 (#123) review across every deploy site. | Keep, principled — the five setup mv-sites (`setup-system-infra.bash`) `chmod` the staged temp then `mv` unconditionally, so the mode is reasserted every run regardless of the backup skip; `deploy_local_logrotate` (`bin/ioc-runner:479-575`) skips the `mv` on identical content but asserts no mode (mktemp default, local user files under `~/.config`, 0600 is correct). Only `do_generate` skips a real mode assertion — fixed in M3, not a Keep. | review 2026-07-28 |
| CI-29 | Extend the atomic staged-rename to the two deploy sites (#120 items 1-2) that the 1.2.0 #107 sweep deliberately excluded: `deploy_local_template`'s `cat > "${template_path}"` (`bin/ioc-runner:441`) and the `install.user` injector's in-place `sed -i` (`configure/inject-runner-version.bash:29-31`). Re-examined in a 1.2.2 M4 three-lens review. | Keep, no meaningful defect. Site 2 is not the torn-write class at all — `sed -i` replaces the inode (temp + rename), so it is already atomic; the only window is a benign partial-stamp state unreachable to a single-user `make install.user`. Site 1 is genuinely non-atomic but its only concurrent reader is `systemd --user`, which reads on `daemon-reload` (after the write), so no torn read is reachable in the single-user `--local install` flow. The stamp-injection 3-point coherence (source decl + both installers) is already guarded by #84/CI-9 (`test_metadata_contract_guard`). Doing items 1-2 buys code-shape uniformity only; recording the Keep stops the next sweep from re-deriving it (this is the second time). Item 3 (SELinux context on setup's `/tmp -> /etc` deploys, RHEL-only) stays open in Backlog, conditional on a production SELinux-enforcing decision. | review 2026-07-28 |
| CI-30 | Do the read-only observers that M5 (#121) item 4 does not fix -- `inspect`, and Site A of the `do_install` group hint (`require_installed_conf:234`) -- share the false-"not found" / mode-blind defect? Examined during the M5 review. | Keep, both unreachable for the real barrier. `inspect` (`bin/ioc-runner:2009`) reaches `resolve_sock_path` (which now carries the shared `assert_conf_dir_readable` guard) only past its system-mode root gate (`:2017`): in system mode root reads the `2770` dir, and in local mode the dir is the caller's own, so the false "not found" never fires for the real cross-user barrier -- `inspect` inherits the shared guard for free, no dedicated fix. `require_installed_conf:234` (Site A) is reached only in system/installed mode, where the `ioc`-group suggestion is correct; local mode routes through `do_install:1576-1577` (Site B, made mode-aware in M5). Both are the seam's principled-Keep halves: M5 single-sources the readability guard for the reachable observers (view/attach/monitor) and the mode-aware hint for the reachable install site, leaving these two as they were. | review 2026-07-28 |

| CI-31 | Should the three version-stamp sites (`bin/setup-system-infra.bash:681`, `bin/ioc-runner:280`, `configure/inject-runner-version.bash:17`) gain a static contract guard pinning their shared working-tree comparison, once M4 (#133) aligns them on `git diff --quiet HEAD --`? Examined 2026-07-31 under the promotion test, in the planning of M4. | Keep, no guard. Elimination is blocked (three self-contained scripts that cannot share a library — the CI-4/CI-15 premise) and gates A and B pass: the three answer one question, and the unguarded comparison shipped wrong in every tag from 1.0.0, 120 commits over those files, silent until the fifth document-only gate run surfaced it. But Gate C fails once M4's own regression asset is netted: that asset drives all three entry points from a relocated clean fixture and a genuinely modified one, as a permanent automated test on both goldens, so a one-sided return to a stat-trusting comparison turns it red with no guard needed. The residual a guard would add — a one-sided move to a comparison those fixtures cannot tell apart, such as one that also counts untracked files — is priced too narrow to fund a fifth guard against a base rate of four promotions in eighteen examined findings. Gate D's placement walk (separate guard, not a fold into `test_metadata_contract_guard`, whose kind is the injected-name contract) was completed before the Gate C netting reversed the outcome and is recorded here so it need not be re-derived if this door reopens. Reopen only on a stamp defect M4's fixtures cannot express. | review 2026-07-31, D6 |
| CI-32 | Should Rocky ordinary IOC operators receive journal access so local lifecycle S29 can run, and should the #140 VM and production matrix remain open to revisit that access? Examined after the 1.2.3 gate recorded six S29 SKIP states. | Keep the least-privilege boundary and close the matrix question. Under this project's Rocky ordinary-user model, S29's journal positive control is not applicable: #17 deliberately removed `systemd-journal` membership because it exposes the whole host journal; #50 established the Rocky user-journal boundary; and 1.2.3 M8 / T4 observed the same six SKIPs. Do not re-grant the group or change host journal policy for this test. Govern the three existing S29 checks with an explicit applicability record and report the whole group as NA on Rocky, never PASS or SKIP. Reopen only if Rocky provides user-unit-only visibility without broad journal access and the owner changes the role model. | owner decision 2026-08-11; this decision commit |
| CI-33 | The local logrotate config's directive set is written in two places — the runner's render (`bin/ioc-runner:589-599`, `weekly / maxsize 50M / rotate 8 / copytruncate / compress / missingok / notifempty / nodateext`) and the test's contract pin (`tests/test-local-lifecycle.bash:1613`, the same eight directives iterated and grepped from the deployed config). Examined in the 1.2.4 conceptual-integrity sweep as the contrast case to the CI-A count seam. | Keep, self-enforcing. Unlike the unguarded count copies, this seam cannot drift silently: if the runner changes a directive (e.g. `rotate 8` to `rotate 12`), `test_local_logrotate` greps the deployed config for the old directive and goes red the same run. The duplication is the guard, not a defect — the test's whole purpose is to pin the render's contract. Reopen only if the directive pin is deleted or moved off the deployed artifact. | review 2026-08-17 |
| CI-34 | The M13/#143 throwaway `--state` isolation appears twice — the runner's `deploy_local_logrotate` (`bin/ioc-runner:632-651`) and the test's own re-validation (`tests/test-local-lifecycle.bash:1622`). Examined in the same sweep. | Keep, not an agreement-point. The two are a mirror, not a seam: each independently passes a mktemp throwaway `--state` so `logrotate -d` never reads the root-owned system default, and they carry no shared value that must match — the runner's succeeds in production, the test's succeeds in the suite. Nothing to keep in sync. Recorded only so the next sweep does not re-derive "why is `--state` in two places". | review 2026-08-17 |
| CI-35 | Add activation-time configuration validation before procServ while preserving the direct procServ `MainPID`, indefinite `Restart=always` policy, shared group-writable configuration, and current account and sudo contracts (M10-2/#102). | Keep the current activation path; do not implement M10-2. systemd 239 cannot distinguish a launch validator's exit from procServ after `exec`, and pinned procServ `073f290` returns child exit codes and errno values, leaving no validation-only numeric status. A parent wrapper would replace procServ as `MainPID` and add another supervisor; validation-time self-stop would expand the sudo contract. The #106 explicit-restart warning remains. Reopen only if the minimum supported systemd gains a source-aware restart decision or the established supervision and privilege contracts change. | owner decision 2026-08-29; this decision commit |
| CI-36 | Add a dedicated `attach` verb check to the container lifecycle suite (S10) so the suite literally covers every verb the T1 plan lists. Examined during the 2026-09-05 container T1 verification on the published 1.0.1 images. | Keep the current coverage; do not add a dedicated attach test. `attach` is tty-bound: the runner execs its default console tool `con` as `con -c <socket>` (`bin/ioc-runner:2390`), and `con` opens `/dev/tty`, which a non-interactive container harness (piped stdin, no controlling terminal) lacks, so it fails with "Can't open /dev/tty" and no input reaches the IOC. `monitor` is testable non-interactively because its read-only path is a unidirectional `socat -u ... STDOUT` that needs no tty, and it stands as the representative console-contract check. Plan item 3 already records attach and monitor as unchanged, mode-agnostic shared code exercised by the other modes' suites; a faithful attach test would need a PTY harness (socat pty/EXEC), materially more complex and OS-variable, for no new container-specific coverage. Reopen only if attach gains a non-tty container path or acquires container-specific logic. | owner decision 2026-09-05; this decision commit |
| CI-37 | Release step 7 would delete `release-1.2.4` (two releases back) and the leftover `release-1.2.3` on origin at the 1.4.0 release. Examined at the 1.4.0 close-out. | Keep both `release-1.2.x` branches on origin; the two-back deletion is not applied to them. Reopen only if the owner asks to prune them. | owner decision 2026-09-06; this decision commit |
| CI-38 | Should `ioc-runner` create `/opt/epics-iocs` and own its default ACL, given that a bare `root:ioc 2775` parent leaves `umask 022` clones at `2755` so `ioc-srv` cannot write IOC runtime artifacts there? Surfaced on an ansible-provisioned host whose iocrunner role applied `chmod 2775` only, with no default ACL. | Keep the boundary: `ioc-runner` does not create `/opt/epics-iocs` or own its ACL. Tree creation and the default ACL (`setfacl -d -m g:ioc:rwx` + `o::rx`, INSTALL.md section 4) belong to the provisioning layer; the ansible-provision repo's iocrunner role added the missing default ACL in commit `39e1704` (same pattern applied to `/opt/epics-pvinfo`). `ioc-runner` keeps only the `install`-time `chdir_conforms_to_system_model` precheck and the documentation (INSTALL.md section 4, USER_GUIDE.md deployment requirement, the install warning). Reopen only if the owner moves tree provisioning into the runner's setup. | owner decision 2026-09-11; this decision commit |
| CI-39 | On a host that resolves sudoers from SSSD/LDAP as well as local files (`sudoers: files sss`), a broad LDAP sudo grant for a user (`(root) ALL`, authenticate) is evaluated after the local drop-in and overrides the `/etc/sudoers.d/10-epics-ioc` `%ioc` NOPASSWD by last-match, so `ioc-runner` prompts for a password despite a correct local policy. Should the runner detect or compensate for SSSD-sourced sudo rules? Surfaced on idev-vsrv1, where the local policy is correct and `#includedir` is the final line (the condition S05 checks — no active rule follows `#includedir` — holds; S05 itself was not run on this host), yet `sudo -k; sudo -n /usr/bin/systemctl daemon-reload` returns exit 1. | Keep the boundary: `ioc-runner` and its system-infra suite inspect only local sudoers; SSSD/LDAP-sourced rules are site identity policy, outside the runner's scope to read or override. S05 (`include-directive-final`, `tests/test-system-infra.bash`) guards local include ordering and by design cannot see SSSD rules. The fix is a site LDAP `sudoRole` granting NOPASSWD for the EPICS commands with a `sudoOrder` above the broad grant (recorded in FAQ Q1). Ordinary operators (`ioc` group, no broad grant) are unaffected. Reopen only if the runner acquires a reason to model non-local sudo sources. | owner decision 2026-09-11; this decision commit |
| CI-40 | The post-init warning sentinel `matching an error pattern` is pinned in four test sites — S31 `_probe_runtime_extra_gate` (`tests/test-local-lifecycle.bash:1561`) and the three S39 probes (`:2484`, `:2491`, `:2505`). Examined in the 1.4.1 M1 (#153) conceptual-integrity sweep. | Keep, self-enforcing (the CI-33 class). The pins are the guard: a change to the warning wording turns all four red in the same run, so the duplication cannot drift silently. Reopen only if a pin is deleted or the sentinel moves off the user-visible text. | review 2026-09-18 |
| CI-41 | The #153 ANSI SGR strip is applied only to the crash-pattern matching path (`crash_scan_filter`), while the readiness-marker byte-offset scan and the death-banner count read the raw window (`read_startup_signals`, `bin/ioc-runner`). Examined in the same sweep. | Keep, principled. The marker offset feeds the caller's post-marker offset arithmetic, so it must be measured on the bytes actually in the file; stripping there would shift every offset. The asymmetry is deliberate and documented at the call site. Reopen only if the offset contract changes. | review 2026-09-18 |
| CI-42 | Does the two-copy unit-template agreement guard (`_unit_must_agree_block` / S16, `tests/test-source-regression.bash:1090`) cover the `EnvironmentFile=-${CONF_DIR}/site.env` line that the runner and setup templates both emit, or does its exclusion list let that line drift? Examined in the 1.4.1 conceptual-integrity sweep. | Keep, guarded. The block-agreement exclusion list is `Description=|Wants=|After=|UMask=|User=|Group=|WantedBy=` — `EnvironmentFile` is not in it, so both scripts' `EnvironmentFile=-${CONF_DIR}/site.env` line stays inside the compared block and a one-sided change turns S16 red the same run. The duplication is pinned, not free to drift. Reopen only if `EnvironmentFile` is added to the exclusion list or the guard is removed. | review 2026-09-19 |
| CI-43 | The register D4 row still describes the superseded colon-marker corroboration rule, while D6 directly below it carries the shipped severity-marker design. Is the stale-looking D4 row a defect? Examined in the same sweep. | Keep, correct decision history. D4 and D6 are append-only decision records, not the current spec: D6 (`docs/milestone-1.4.1.md:43`) opens "D4's corroborating-token mechanism is superseded", and the plan's Superseded Plan Artifacts line names the D4 colon-marker step. Rewriting D4 to match D6 would erase the very supersession the pair records; ADR 0004 holds the current rationale. Reopen only if a reader could mistake D4 for current guidance despite D6. | review 2026-09-19 |

CI-1 through CI-24 were recorded in the register of the cycle that closed them:
`git show 1.2.0:docs/milestone.md`. The history answers directly too —
`git log --grep=examined-Keep`.
